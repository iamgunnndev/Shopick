import { GallaryDirective } from './gallary.directive';

describe('GallaryDirective', () => {
  it('should create an instance', () => {
    const directive = new GallaryDirective();
    expect(directive).toBeTruthy();
  });
});
